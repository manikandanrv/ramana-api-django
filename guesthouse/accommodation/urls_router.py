# from django.urls import path
# from rest_framework.urlpatterns import format_suffix_patterns
# from accommodation import viewsets
# from django.conf.urls import url, include
# from rest_framework.routers import DefaultRouter


# router = DefaultRouter()
# router.register(r'devotees', viewsets.DevoteeViewSet)
# router.register(r'users', viewsets.DevoteeViewSet)
# router.register(r'requests', viewsets.DevoteeViewSet)
# router.register(r'rooms', viewsets.DevoteeViewSet)
# router.register(r'checkins', viewsets.DevoteeViewSet)

# urlpatterns = [
#     url(r'^', include(router.urls)),
# ]
